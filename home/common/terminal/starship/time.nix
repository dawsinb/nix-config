colors:

{
    settings = {
        time = {
            disabled = false;
            time_format = "%R";
            style = "bg:${colors.bg}";
            format = "[[   $time ](fg:${colors.fg} bg:${colors.bg})]($style)";
        };
    };

    formatString = "$time";
}